---
title: 实践-(一)创建简单的http服务器
description: 使用 Go 语言 net/http 包搭建简单 HTTP 服务器，介绍 ServeMux 路由配置、Handler 函数绑定以及 Server 结构体的基本用法。
seo:
  title: Go HTTP 服务器搭建教程 - net/http 包使用指南
  description: 使用 Go 语言 net/http 包搭建简单 HTTP 服务器，介绍 ServeMux 路由配置、Handler 函数绑定以及 Server 结构体的基本用法，适合 Go 语言网络编程入门学习。
navigation: Golang
---

## 说在前面
> - 实验环境：[【go/vscode】远程环境搭建(离线版/gopls版)](https://blog.csdn.net/qq_33446100/article/details/107695641)
> - 客户端环境：chrome 84.0.4
> - 俩本好书：[Go语言高级编程](http://books.studygolang.com/advanced-go-programming-book/)、[Go语言圣经](https://docs.hacknode.org/gopl-zh/index.html)
## 实验要求
- 搭建一个简单的http服务器，可以对http请求进行响应，例如：
	~~~shell
	http://127.0.0.1:8080/add?text=123456
	~~~
	页面返回
	~~~shell
	123456
	~~~

## 代码
- 实验较为简单，直接上代码
	~~~go
	package main
	
	import (
		"fmt"
		"log"
		"net/http"
	)
	
	func RecordHandler(w http.ResponseWriter, r *http.Request) {
		values := r.FormValue("content")
		if values == "" {
			fmt.Fprintf(w, "try to use \"/echo?content=xxx!\"")
			return
		}
	
		fmt.Fprintf(w, values)
	}
	
	func main() {
		mux := http.NewServeMux()
		mux.HandleFunc("/echo", RecordHandler)
	
		server := &http.Server{
			Addr:    ":8081",
			Handler: mux,
		}
	
		err := server.ListenAndServe()
		if err != nil {
			if err == http.ErrServerClosed {
				log.Print("Server closed under request")
			} else {
				log.Fatal("Server closed unexpected")
			}
		}
	}
	~~~

## 分析
- 如果使用其他框架，例如`nodejs`或者基于`python`的`pyweb`，其实会发现这个构建步骤十分相似，基本上是创建服务，构建路由并绑定`handler`，然后开启监听。
- 想要了解具体的实现请参考[学习用Go编写HTTP Server](https://www.jianshu.com/p/4a560e0cce22)以及[golang 构建HTTP服务](https://blog.csdn.net/lengyuezuixue/article/details/79094323)
- [`http.Request`](https://golang.org/pkg/net/http/#Request)

## 实验结果
![在这里插入图片描述](/golang/6.png)
![在这里插入图片描述](/golang/7.png)



## 参考资料
- [学习用Go编写HTTP Server](https://www.jianshu.com/p/4a560e0cce22)
- [Go 网络编程：使用 Handler 和 HandlerFunc](https://www.jianshu.com/p/3b5c4fc0695c)
- [golang 构建HTTP服务](https://blog.csdn.net/lengyuezuixue/article/details/79094323)