---
title: vllm-在内网wsl上安装vllm并尝试多节点部署(失败了)
description: 在内网 WSL2 环境下安装 vLLM 并尝试多节点分布式部署的实践记录，包括 miniconda 配置、conda-pack 离线迁移、Ray 集群搭建以及遇到的各种问题与解决方案。
seo:
  title: 【瞎折腾】vLLM内网WSL多节点部署实践(失败记录)
  description: 记录在内网 WSL2 环境下安装 vLLM 并尝试多节点分布式部署的完整过程，包括 miniconda 虚拟环境配置、conda-pack 离线打包迁移、Ray 集群搭建、CUDA 环境配置以及遇到的各种问题与解决方案。
navigation: 瞎折腾
---

## 说在前面
> - 操作系统(外网)：win11 wsl2
> - miniconda版本：25.1.1 （该版本的python版本为3.12.9）
> - 操作系统(内网节点一)：win10 wsl 4060ti 16g
> - 操作系统(内网节点二)：win10 wsl 2070s 8g
> - 其他：llama.cpp它不香吗，折腾这个？
## miniconda安装
- [官网](https://www.anaconda.com/docs/getting-started/miniconda/install)
- 安装
	~~~shell
	# 最好到用户目录下创建文件夹
	cd ~
	# 创建一个目录
	mkdir miniconda3 
	cd miniconda3
	# 下载安装脚本 
	wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O .miniconda.sh
	bash miniconda.sh -b -u -p .
	~~~
- 创建虚拟环境
	~~~shell
	conda create -n vllm
	conda activate vllm
	~~~
- 注意不要在`base`环境中直接安装`vllm`

## 外网创建虚拟环境
### 创建节点一的虚拟环境
- 由于`ray`的集群需要保持`ray`版本以及`python`版本一致，而节点二的机器比较老，所以这里选择的`python`版本为3.10
- 创建虚拟环境
	~~~shell
	conda create -n vllm1 python=3.10
	conda activate vllm1
	pip install vllm
	~~~
- 节点一的cuda环境比较新，为12.x，所以直接安装vllm即可
- 安装完后，使用`conda-pack`打包
	~~~shell
	pip install conda-pack
	conda-pack
	~~~
- 注意，这里安装的`ray`版本为`2.44.1`
- 注意不要在`base`环境中打包
### 创建节点二的虚拟环境
- 创建虚拟环境
	~~~shell
	conda create -n vllm2  python=3.10
	conda activate vllm2
	~~~
- 节点二的cuda版本为11.x，需要指定版本安装
	~~~shell
	export VLLM_VERSION=0.6.1.post1
	export PYTHON_VERSION=310
	pip install https://github.com/vllm-project/vllm/releases/download/v${VLLM_VERSION}/vllm-${VLLM_VERSION}+cu118-cp${PYTHON_VERSION}-cp${PYTHON_VERSION}-manylinux1_x86_64.whl --extra-index-url https://download.pytorch.org/whl/cu118
	~~~
- 这样安装后的`ray`版本为`2.44.0`，与节点一不一致，要升个级
	~~~shell
	pip install --upgrade ray==2.44.1
	~~~
- 同样，安装完后，使用`conda-pack`打包
	~~~shell
	pip install conda-pack
	conda-pack
	~~~
- 注意不要在`base`环境中打包

## 内网配置环境
### 配置节点一的环境
- 导入打包后的虚拟环境到内网
- 解压
	~~~shell
	mkdir vllm
	tar -xzf vllm1.tar.gz -C vllm
	~~~
- 激活虚拟环境
	~~~shell
	source vllm/bin/activate
	~~~
- 还原虚拟环境
	~~~shell
	conda-unpack 
	~~~

### 配置节点二的环境
- 操作与上一步一致


## vllm多节点配置
### 节点一(主节点)配置
- 先进入虚拟环境
	~~~shell
	conda activate vllm
	~~~
- 确定本机IP
	~~~shell
	hostname -I
	xxx.xxx.xxx.xxx
	~~~
- 启用ray
	~~~shell
	VLLM_HOST_IP=xxx.xxx.xxx.xxx ray start --head --block --port 6379 --num-gpus=1 --resources='{"node:{xxx.xxx.xxx.xxx}":1}'
	# num-gpus 表示本节点的gpu数量 折腾过程中有次填了个2 直接一堆奇奇怪怪的问题
	~~~
### 节点二配置
- 先进入虚拟环境
	~~~shell
	conda activate vllm
	~~~
- 确定本机IP
	~~~shell
	hostname -I
	yyy.yyy.yyy.yyy
	~~~
- 启用ray
	~~~shell
	VLLM_HOST_IP=yyy.yyy.yyy.yyy ray start --block --address xxx.xxx.xxx.xxx:6379 --num-gpus=1 --resources='{"node:{yyy.yyy.yyy.yyy}":1}'
	~~~
## 部署LLM(失败)
- 惨遭滑铁卢，以下可忽略，不是主要原因
	> - wsl不支持`SO_REUSADDR`，而ray的cluster通信用的是`grpc`
	> - [相关issue1](https://github.com/ray-project/ray/issues/6832)
	> - [相关issue2](https://github.com/grpc/grpc/pull/13517/commits/daefb8740401ab048dd759045dca6c7f97054b92)
	> - 不过看样子好像ray解决了这个问题
- 可能还是wsl2的网络设置有问题，两边访问不到一块
## 补充：wsl2设置ulimit
- 编辑`/etc/security/limits.conf`
	~~~shell
	sudo vi /etc/security/limits.conf
	~~~
- 添加以下内容
	~~~shell
	*	soft	nofile	65535
	*	hard	nofile	65535
	~~~
- 重新su一下
	~~~shell
	su 用户名
	# 输入密码
	ulimit -n
	~~~

## 补充：wsl安装libcuda
- 正常来讲，新版的显卡驱动默认支持wsl2
![在这里插入图片描述](./tinkering/19.webp)
- 如果是老版本的，可能需要自己手动安装下，[下载页](https://developer.nvidia.com/cuda-toolkit-archive)
![在这里插入图片描述](./tinkering/18.webp)
不要选错了 ~~（装完windows版本的发现还是不行，吐血三升）~~ 

- 例如
	~~~shell
	wget https://developer.download.nvidia.com/compute/cuda/11.2.0/local_installers/cuda_11.2.0_460.27.04_linux.run
	sudo sh cuda_11.2.0_460.27.04_linux.run
	~~~

## 补充：wsl2端口绑定
- [这里](https://learn.microsoft.com/zh-cn/windows/wsl/networking)

## 问题
- 在创建环境`conda --no-plugins create -n vllm python=3.10`时出现`OSError: [Errno 40] Too many levels of symbolic links:`
  `miniconda`安装的时候不要安装在`/mnt`目录下，安装到`/home/user`下
- 在安装`vllm`时出现`PermissionError: [Errno 13] Permission denied:`
  同上，`miniconda`安装的时候不要安装在`/mnt`目录下，安装到`/home/user`下
- `libcuda.so.1: cannot open shared object file: no such file or directory`
  `cuda tookit`没有安装，去[官网](https://developer.nvidia.com/cuda-toolkit-archive)选择对应版本安装
  注意，windows主机上如果安装了最新的显卡驱动，那么正常来讲就会在`/usr/lib/wsl/lib`下生成对应的`libcuda.so`，但是，重点来了，**<font color=red>必须先装显卡驱动，再安装linux发行版本**</font>
- 遇到一堆`ray`的问题，大部分和自己的配置有关，所以`vllm`上的教程直接让用`docker`，真难折腾
  - [[Installation]: pynvml.NVMLError_InvalidArgument: Invalid Argument](https://github.com/vllm-project/vllm/issues/9865)
  - [error helper for TypeError: _extractNVMLErrorsAsClasses..gen_new..new() takes 1 positional argument but 2 were given](https://github.com/vllm-project/vllm/issues/12906)
  - [Error: No available node types can fulfill resource request](https://github.com/vllm-project/vllm/issues/7815)
## 吐槽
- `vllm`多节点真不好用，不像`llama.cpp`那样开箱即用，主要依赖`ray`，这个东东真的一言难尽